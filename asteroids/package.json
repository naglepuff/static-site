{
  "name": "naglepuff/asteroids",
  "version": "1.0.0",
  "description": "A pure javascript implementation of asteroids.",
  "author": "naglepuff",
  "license": "ISC",

  "main": "index.js",
  "scripts": {
    "build": "esbuild src/index.js --bundle --outfile=dist/bundle.js",
    "format": "prettier --write ./src/"
  },

  "devDependencies": {
    "esbuild": "^0.28.0",
    "prettier": "^3.8.2"
  }
}
